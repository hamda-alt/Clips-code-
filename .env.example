# ---- MySQL connection ----
# Point these at your MySQL 8 server. The schema lives in db/schema.sql.
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=clips

# Public base URL used when generating shareable clip links.
NEXT_PUBLIC_APP_URL=http://localhost:3000
